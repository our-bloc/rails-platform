<%= form_tag(influencers_path, :method => "get") do %>
 <div class="search_field"  >
      <%= text_field_tag :search, params[:search], placeholder: "Search for more success stories here " %>
</div>
  <% end %>
  <%= yield %>