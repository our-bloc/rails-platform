<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col s12 m6">
    <div class="card grey darken-2">
      <div class="card-content white-text">
        <span class="card-title"><%= @resource.title %>, <%= @resource.sector %></span>
        <p><%= @resource.body %></p>
      </div>
      <div class="card-action">
        <a href="#"><%= link_to 'Edit', edit_resource_path(@resource) %></a>
        <a href="#"><%= link_to 'Back', resources_path %></a>
      </div>
    </div>
  </div>
</div>
