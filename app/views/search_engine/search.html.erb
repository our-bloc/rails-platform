
 
  <% if params[:search] != nil  %> 
    <%= render 'search_engine/roadmap' %> 
    
  <% else %> 

  <div class="top-div" style="margin-bottom:100em; padding: 2%; position:absolute; width: 100%; left: 0; top:-5%" >

  <img src="/img/bgman2.png" style="z-index: -10; left: 0%; position:absolute; width: 100%; overflow: hidd"> 
    <h5 style="margin: 2%; position: absolute; font-weight: 800"> blacpac.</h5>
     <h5 class="header white-text" style="font-weight: 800;margin-top: 10%">Your college resource finder </h5>
     <h6 class="search_bullets center " style="font-weight: 800"> Tell us where you want to go to school and a little about yourself we'll give you ...</h6>
     <h6 class="search_bullets center " style="font-weight: 800"> A personalized resume to show off </h6>


    <%= form_tag(search_path, :method => "get") do %>
         <div class="search_field"  >
            <% if @new_input == nil %> 
              <%= text_field_tag :search, params[:search], placeholder: "Where do you want to go school?", 
              :class => "search_input" %>
            <% else %> 
              <%= text_field_tag :search, params[:search], value: @new_input, 
              :class => "search_input" %>
            <% end %> 
         </div>
      
       <h6 class="search_bullets recent" style="margin-left: -30em"> <span style="font-weight: 800">recent searches </span>
      <% @recent_searches[0..3].each do |search| %> 
        <span class="recent_search"  id="<%= search.query %>"> <%= link_to search.query %> &bull; </span>
        
        
      <% end %> 
      </h6>  
      
 </div>
 
 <!-- render quiz for search engine --> 
    <%= render 'search_engine/quiz' %> 
  
  
  
  
  <% end %>
  <% end %> 

  

<%= render 'search_engine/scriptstyle' %> 


<style> 
html,body {
  width: 100%
}
.top-div .header, .top-div form  {
  width: 100%;
  text-align: center;
}

.top-div form {
  left: 22%;
}
.recent {
  left: 0;
}

.search_field input::placeholder{
  color: white;
}
</style>