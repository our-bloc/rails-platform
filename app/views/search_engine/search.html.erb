
 
  <% if params[:search] != nil  %> 
    <%= render 'search_engine/roadmap' %> 
    
  <% else %> 
  <div class="top-div" style="margin-bottom:100em; padding: 2%" >
     <h5 class="header" style="font-weight: 800;margin-top: 10%">Without the right resume & cover letter you only have a 1.2% of landing your next internship</h5>
     <h6 class="search_bullets" style="font-weight: 800"> Tell us what internship you want, a little about yourself and we'll give you </h6>
     <h6 class="search_bullets"> Personalized, Typo Free & Perfectly Formatted Resume &#10004 </h6>
     <h6 class="search_bullets"> Your go-to Ultimate Cover Letter Template &#10004 </h6>
     <h6 class="search_bullets"> A Stand Out LinkedIN Bio &#10004 </h6>

    <%= form_tag(search_path, :method => "get") do %>
         <div class="search_field"  >
            <% if @new_input == nil %> 
              <%= text_field_tag :search, params[:search], placeholder: "What opportunity are you looking for?", 
              :class => "search_input" %>
            <% else %> 
              <%= text_field_tag :search, params[:search], value: @new_input, 
              :class => "search_input" %>
            <% end %> 
         </div>
      
       <h6 class="search_bullets"> <span style="font-weight: 800">recent searches </span>
      <% @recent_searches[0..3].each do |search| %> 
        <span class="recent_search" id="<%= search.query %>"> <%= link_to search.query %> &bull; </span>
        
        
      <% end %> 
      </h6>  
      
 </div>
 
 <!-- render quiz for search engine --> 
    <%= render 'search_engine/quiz' %> 
  
  
  
  
  <% end %>
  <% end %> 

  

<%= render 'search_engine/scriptstyle' %> 
